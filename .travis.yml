language: node_js
cache:
  directories:
    - ~/.npm
dist: trusty
sudo: required
notifications:
  email: false
node_js: 10
addons:
  chrome: stable
before_script:
  - npm i -g polymer-cli
branches:
  except:
    - /^v\d+\.\d+\.\d+$/
deploy:
  provider: script
  on:
    branch: legacy
  skip_cleanup: true
  script:
    - npx semantic-release --branch legacy